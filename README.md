
# Django-Seminar – fortlaufendes Beispielprojekt

Dieses Repo entsteht blockweise im Seminar. Pro Unterrichtsblock gibt es genau einen Git-Commit.

## Start
```bash
python -m venv venv
source venv/bin/activate
pip install django
python manage.py migrate
python manage.py runserver
```
